<h2>New house</h2>

<%= @current_user.name %>

<%= render "form.html", changeset: @changeset,
                        action: house_path(@conn, :create) %>

<%= link "Back", to: house_path(@conn, :index) %>
