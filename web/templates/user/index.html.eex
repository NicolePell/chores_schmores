<h2>ChoresSchmores Users!</h2>

<table class="table">
  <%= for user <- @users do %>
  <tr>
    <td><%= first_name(user) %> (<%= user.id %>)</td>
    <td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
  </tr>
  <% end %>
</table>
